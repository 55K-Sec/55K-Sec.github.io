# 安全基线检查工具 - 依赖清单

# === 核心依赖（当前项目可能仅需标准库，但为兼容性和扩展性保留） ===
# 本项目主要使用 Python 标准库，因此无强制第三方依赖
# 但为支持未来功能或增强体验，建议保留以下可选依赖

# === 可选：报告美化与导出 ===
# jinja2>=3.0            # 用于 HTML 报告模板渲染（如果 html_reporter 使用模板）
# weasyprint>=57.0       # 将 HTML 转为 PDF（高级需求，非必需）

# === 可选：日志增强 ===
# colorlog>=6.0          # 彩色终端日志输出（提升 CLI 体验）

# === 可选：配置管理 ===
# pyyaml>=6.0            # 如果未来支持 YAML 格式配置文件

# === 开发/测试依赖（通常放在 requirements-dev.txt）===
# pytest>=7.0
# black>=23.0
# flake8>=6.0

# 当前版本无强制第三方依赖，纯标准库实现